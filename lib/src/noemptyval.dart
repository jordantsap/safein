
import 'package:flutter/cupertino.dart';

class NoEmptyVal  extends StatelessWidget {
  // final output = AppLocalizations.of(context)
  //     .translate('notempty_val');

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("rhtyjhrte"),
    );
  }
}
